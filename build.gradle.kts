plugins {
}

group = "it.unibo"
//version = "1.0-SNAPSHOT"

repositories {
    mavenCentral()
}

dependencies {
}

// TODO: CREATE A TASK THAT BUILDS EVERY DOCKERFILE IN THE PROJECT (INSIDE MINIKUBE CLUSTER)
// TODO: CREATE A TASK THAT DEPLOYS THE WHOLE PROJECT IN THE CLUSTER
