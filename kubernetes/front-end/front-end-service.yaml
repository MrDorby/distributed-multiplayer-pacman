apiVersion: v1
kind: Service
metadata:
  name: front-end
  labels:
    app: pacman-game
spec:
  type: LoadBalancer
  ports:
    - port: 8080
  selector:
    app: pacman-game
    role: front-end-gateway
