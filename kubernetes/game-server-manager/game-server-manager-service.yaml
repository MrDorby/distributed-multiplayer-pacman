apiVersion: v1
kind: Service
metadata:
  name: game-server-manager
  labels:
    app: pacman-game
spec:
  type: NodePort
  ports:
    - port: 8080
  selector:
    app: pacman-game
    role: game-server-manager
